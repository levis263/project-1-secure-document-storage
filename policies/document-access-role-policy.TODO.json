{
  "_comment": "PLACEHOLDER — do not commit as-is. Replace with the actual saved CrossAccountDocumentRolePolicy JSON (Policies > CrossAccountDocumentRolePolicy > JSON tab). Confirmed so far: s3:GetObject on arn:aws:s3:::project1-sensitive-data/*. kms:Decrypt is described in the architecture write-up but not yet captured from the console in its final saved form. IAM policy syntax note: kms:Decrypt needs its own statement with the KMS key ARN as Resource — it cannot share a Resource field with the S3 statement, since the two actions take different resource types.",
  "Version": "2012-10-17",
  "Statement": [
    {
      "Sid": "REPLACE_WITH_REAL_STATEMENTS",
      "Effect": "Allow",
      "Action": "REPLACE",
      "Resource": "REPLACE"
    }
  ]
}
